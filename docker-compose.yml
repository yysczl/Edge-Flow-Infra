services:
  cloudflared:
    image: cloudflare/cloudflared:latest
    container_name: edge-flow-cloudflared
    network_mode: host
    restart: unless-stopped
    command:
      - tunnel
      - --no-autoupdate
      - run
      - --token
      - ${CLOUDFLARED_TOKEN}
